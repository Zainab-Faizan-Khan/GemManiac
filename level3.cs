using System.Collections;
using System.Collections.Generic;
using UnityEngine;
public class level003 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        RedirectLevel.redirectToLevel = 7;
    }

  
}
